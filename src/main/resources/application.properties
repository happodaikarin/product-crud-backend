# Elimina o comenta la siguiente l�nea si deseas usar MySQL
# app.use-mysql=false

# Configuraci�n de la base de datos
spring.datasource.url=${DB_URL:jdbc:mysql://mysql-db:3306/productdb?allowPublicKeyRetrieval=true&useSSL=false}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:newpassword}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# Configuraci�n de JPA/Hibernate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Configuraci�n de CORS (opcional si configuras en WebConfig.java)
# spring.web.cors.allowed-origins=*
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
